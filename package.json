{
  "name": "mazhaivaanam-boutique",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix backend"
  }
}
